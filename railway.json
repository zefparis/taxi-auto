{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && NODE_ENV=production NODE_OPTIONS=--max_old_space_size=1024 npm ci --omit=dev --prefer-offline --no-audit --progress=false && npm run build"
  },
  "deploy": {
    "startCommand": "cd frontend && npm start"
  }
}
